info:
  name: one trip item
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/trip-items/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
