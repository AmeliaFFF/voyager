info:
  name: all trip items
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/admin/trip-items
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
