# Voyager

Voyager is a MERN travel planning application. This repository is structured as a monorepo with separate folders for the backend, frontend, and Bruno API testing collection.

The current completed assessment work is the backend API, located in the `backend` folder.